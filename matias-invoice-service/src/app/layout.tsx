export const metadata = {
  title: 'Matias Invoice Service',
  description: 'Microservicio de facturación electrónica con Matias',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

