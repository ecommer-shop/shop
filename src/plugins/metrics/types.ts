/**
 * @description
 * Options for the MetricsDashboardPlugin (React Dashboard widget).
 * The server-side metrics are handled by @pinelab/vendure-plugin-metrics.
 */
export interface MetricsDashboardPluginOptions {
    // No additional options needed - the dashboard widget reads from the
    // GraphQL API exposed by @pinelab/vendure-plugin-metrics
}
