export enum Route {
  ADMIN = "admin",
  ADMIN_API = "admin-api",
  ASSETS = "assets",
  MAILBOX = "mailbox",
  SHOP = "shop",
  SHOP_API = "shop-api",
}

export enum RouteStore {
  verify = "/verify",
  passwordReset = "/password-reset",
  changeEmailAddress = "/verify-email-address-change",
}
